// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: spec/v1/device.proto

package v1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/stupside/DATA-intensive/assignment-4/proto/gen/spec/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// DeviceServiceName is the fully-qualified name of the DeviceService service.
	DeviceServiceName = "spec.v1.DeviceService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// DeviceServiceConnectionsProcedure is the fully-qualified name of the DeviceService's Connections
	// RPC.
	DeviceServiceConnectionsProcedure = "/spec.v1.DeviceService/Connections"
)

// DeviceServiceClient is a client for the spec.v1.DeviceService service.
type DeviceServiceClient interface {
	// Connections returns connection history for authenticated device
	Connections(context.Context, *connect.Request[v1.ConnectionsRequest]) (*connect.Response[v1.ConnectionsResponse], error)
}

// NewDeviceServiceClient constructs a client for the spec.v1.DeviceService service. By default, it
// uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewDeviceServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) DeviceServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	deviceServiceMethods := v1.File_spec_v1_device_proto.Services().ByName("DeviceService").Methods()
	return &deviceServiceClient{
		connections: connect.NewClient[v1.ConnectionsRequest, v1.ConnectionsResponse](
			httpClient,
			baseURL+DeviceServiceConnectionsProcedure,
			connect.WithSchema(deviceServiceMethods.ByName("Connections")),
			connect.WithClientOptions(opts...),
		),
	}
}

// deviceServiceClient implements DeviceServiceClient.
type deviceServiceClient struct {
	connections *connect.Client[v1.ConnectionsRequest, v1.ConnectionsResponse]
}

// Connections calls spec.v1.DeviceService.Connections.
func (c *deviceServiceClient) Connections(ctx context.Context, req *connect.Request[v1.ConnectionsRequest]) (*connect.Response[v1.ConnectionsResponse], error) {
	return c.connections.CallUnary(ctx, req)
}

// DeviceServiceHandler is an implementation of the spec.v1.DeviceService service.
type DeviceServiceHandler interface {
	// Connections returns connection history for authenticated device
	Connections(context.Context, *connect.Request[v1.ConnectionsRequest]) (*connect.Response[v1.ConnectionsResponse], error)
}

// NewDeviceServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewDeviceServiceHandler(svc DeviceServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	deviceServiceMethods := v1.File_spec_v1_device_proto.Services().ByName("DeviceService").Methods()
	deviceServiceConnectionsHandler := connect.NewUnaryHandler(
		DeviceServiceConnectionsProcedure,
		svc.Connections,
		connect.WithSchema(deviceServiceMethods.ByName("Connections")),
		connect.WithHandlerOptions(opts...),
	)
	return "/spec.v1.DeviceService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case DeviceServiceConnectionsProcedure:
			deviceServiceConnectionsHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedDeviceServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedDeviceServiceHandler struct{}

func (UnimplementedDeviceServiceHandler) Connections(context.Context, *connect.Request[v1.ConnectionsRequest]) (*connect.Response[v1.ConnectionsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("spec.v1.DeviceService.Connections is not implemented"))
}
