// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: spec/v1/relay.proto

package v1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/stupside/DATA-intensive/assignment-4/modules/proto/gen/spec/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// RelayServiceName is the fully-qualified name of the RelayService service.
	RelayServiceName = "spec.v1.RelayService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// RelayServiceStreamProcedure is the fully-qualified name of the RelayService's Stream RPC.
	RelayServiceStreamProcedure = "/spec.v1.RelayService/Stream"
	// RelayServiceSummaryProcedure is the fully-qualified name of the RelayService's Summary RPC.
	RelayServiceSummaryProcedure = "/spec.v1.RelayService/Summary"
	// RelayServiceConsumeProcedure is the fully-qualified name of the RelayService's Consume RPC.
	RelayServiceConsumeProcedure = "/spec.v1.RelayService/Consume"
)

// RelayServiceClient is a client for the spec.v1.RelayService service.
type RelayServiceClient interface {
	// Stream is used by the producer to push data chunks (bidirectional with backpressure)
	Stream(context.Context) *connect.BidiStreamForClient[v1.StreamRequest, v1.StreamResponse]
	// Summary provides relay statistics
	Summary(context.Context, *connect.Request[v1.SummaryRequest]) (*connect.Response[v1.SummaryResponse], error)
	// Consume is used by the consumer to pull data chunks (bidirectional with backpressure)
	Consume(context.Context) *connect.BidiStreamForClient[v1.ConsumeRequest, v1.ConsumeResponse]
}

// NewRelayServiceClient constructs a client for the spec.v1.RelayService service. By default, it
// uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewRelayServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) RelayServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	relayServiceMethods := v1.File_spec_v1_relay_proto.Services().ByName("RelayService").Methods()
	return &relayServiceClient{
		stream: connect.NewClient[v1.StreamRequest, v1.StreamResponse](
			httpClient,
			baseURL+RelayServiceStreamProcedure,
			connect.WithSchema(relayServiceMethods.ByName("Stream")),
			connect.WithClientOptions(opts...),
		),
		summary: connect.NewClient[v1.SummaryRequest, v1.SummaryResponse](
			httpClient,
			baseURL+RelayServiceSummaryProcedure,
			connect.WithSchema(relayServiceMethods.ByName("Summary")),
			connect.WithClientOptions(opts...),
		),
		consume: connect.NewClient[v1.ConsumeRequest, v1.ConsumeResponse](
			httpClient,
			baseURL+RelayServiceConsumeProcedure,
			connect.WithSchema(relayServiceMethods.ByName("Consume")),
			connect.WithClientOptions(opts...),
		),
	}
}

// relayServiceClient implements RelayServiceClient.
type relayServiceClient struct {
	stream  *connect.Client[v1.StreamRequest, v1.StreamResponse]
	summary *connect.Client[v1.SummaryRequest, v1.SummaryResponse]
	consume *connect.Client[v1.ConsumeRequest, v1.ConsumeResponse]
}

// Stream calls spec.v1.RelayService.Stream.
func (c *relayServiceClient) Stream(ctx context.Context) *connect.BidiStreamForClient[v1.StreamRequest, v1.StreamResponse] {
	return c.stream.CallBidiStream(ctx)
}

// Summary calls spec.v1.RelayService.Summary.
func (c *relayServiceClient) Summary(ctx context.Context, req *connect.Request[v1.SummaryRequest]) (*connect.Response[v1.SummaryResponse], error) {
	return c.summary.CallUnary(ctx, req)
}

// Consume calls spec.v1.RelayService.Consume.
func (c *relayServiceClient) Consume(ctx context.Context) *connect.BidiStreamForClient[v1.ConsumeRequest, v1.ConsumeResponse] {
	return c.consume.CallBidiStream(ctx)
}

// RelayServiceHandler is an implementation of the spec.v1.RelayService service.
type RelayServiceHandler interface {
	// Stream is used by the producer to push data chunks (bidirectional with backpressure)
	Stream(context.Context, *connect.BidiStream[v1.StreamRequest, v1.StreamResponse]) error
	// Summary provides relay statistics
	Summary(context.Context, *connect.Request[v1.SummaryRequest]) (*connect.Response[v1.SummaryResponse], error)
	// Consume is used by the consumer to pull data chunks (bidirectional with backpressure)
	Consume(context.Context, *connect.BidiStream[v1.ConsumeRequest, v1.ConsumeResponse]) error
}

// NewRelayServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewRelayServiceHandler(svc RelayServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	relayServiceMethods := v1.File_spec_v1_relay_proto.Services().ByName("RelayService").Methods()
	relayServiceStreamHandler := connect.NewBidiStreamHandler(
		RelayServiceStreamProcedure,
		svc.Stream,
		connect.WithSchema(relayServiceMethods.ByName("Stream")),
		connect.WithHandlerOptions(opts...),
	)
	relayServiceSummaryHandler := connect.NewUnaryHandler(
		RelayServiceSummaryProcedure,
		svc.Summary,
		connect.WithSchema(relayServiceMethods.ByName("Summary")),
		connect.WithHandlerOptions(opts...),
	)
	relayServiceConsumeHandler := connect.NewBidiStreamHandler(
		RelayServiceConsumeProcedure,
		svc.Consume,
		connect.WithSchema(relayServiceMethods.ByName("Consume")),
		connect.WithHandlerOptions(opts...),
	)
	return "/spec.v1.RelayService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case RelayServiceStreamProcedure:
			relayServiceStreamHandler.ServeHTTP(w, r)
		case RelayServiceSummaryProcedure:
			relayServiceSummaryHandler.ServeHTTP(w, r)
		case RelayServiceConsumeProcedure:
			relayServiceConsumeHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedRelayServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedRelayServiceHandler struct{}

func (UnimplementedRelayServiceHandler) Stream(context.Context, *connect.BidiStream[v1.StreamRequest, v1.StreamResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("spec.v1.RelayService.Stream is not implemented"))
}

func (UnimplementedRelayServiceHandler) Summary(context.Context, *connect.Request[v1.SummaryRequest]) (*connect.Response[v1.SummaryResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("spec.v1.RelayService.Summary is not implemented"))
}

func (UnimplementedRelayServiceHandler) Consume(context.Context, *connect.BidiStream[v1.ConsumeRequest, v1.ConsumeResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("spec.v1.RelayService.Consume is not implemented"))
}
